# 质量与成本小试：预先冻结的协议

协议配置由prepare_quality_cost_pilot.py于2026-09-15T16:39:02.207153+00:00在训练开始前生成；此说明于2026-09-15T16:41:33.705798+00:00补记。配置SHA256：c59143379b453587e139069e526482c2048a24b9843c8087659ef8824f713d07。

## 要回答的问题

1. 同步修复在默认反向下是否基本保持原算法的训练结果和耗时？比较original_k4和barrier_k4，固定原版池化。
2. FP32池化是否改变效果/开销？比较barrier_k4和fp32_k4，只改变池化精度。
3. 稀疏注意力是否可以在接近dense质量时减少完整训练耗时？比较dense、fp32_k4、fp32_k16。

所有条件都是已有算法及数值修复，不称为新ML方法。同步修复过关允许进入这个小试，不能作为质量增益证据。

## 固定设置

- Qwen2.5-0.5B base，固定revision060db6499f32faf8b98477b0a26969ef7d8b9987；24层Q/K/V/O投影rank8、alpha16 LoRA，底座冻结。
- FP32主干/LoRA，BF16注意力；默认反向，关闭TF32。没有量化或全参数预训练。
- 8K上下文，B128，K含强制当前块；池化BN32、warps4、stages3。
- 两种子[2026091552, 2026091553]，每条件64步，10条轨迹640更新，单轨迹524288训练token。数据顺序对所有条件/种子相同，种子只用于初始化。
- AdamW恒定lr1e-4，betas(0.9,0.999)、eps1e-8、weight decay0；dropout0，无梯度裁剪和检查点。
- 第一种子条件顺序dense→original_k4→barrier_k4→fp32_k4→fp32_k16；第二种子反序，控制部分顺序/温度影响。

## 数据与判定

WikiText2固定revision b08601e04326c79dfdd32d625aee71d232d685c3，自然换行拼接，无人工EOS。训练与验证split分开。以8K+1窗口组成移位预测；训练64个窗口，校准4个，报告16个，索引与token SHA完整保存。排除前两次池化/同步诊断使用过的重叠窗口；这不表示排除整个项目历史上所有使用，更不表示全新外部分布。

校准在第0、16、32、64步；最终报告只在第64步。训练步数、学习率、K以及种子在运行前固定，不根据报告窗口更改。所有最终16窗口分数都保留，报告逐窗口配对差及两种子范围。

预先筛选为：两种子相对对应dense的报告NLL差均≤0.03nats（约3.05%困惑度差），训练稳态中位步耗时比均≤0.95。该门槛用于控制是否扩大，不作为正式统计非劣性证明。只有2种子/16个相关文本窗口，不夸大置信区间。

## 时间记录

每步同步墙钟时间包括token搬入、前向、反向、有限梯度检查和优化器，所有条件一致。另存总训练步耗时以及去除前4步后的中位数。模型加载、校准/报告评测、写checkpoint不计入训练步时间，但完整作业墙钟时间也保留。源码、实际扩展SHA、初始化tensor SHA、token SHA固定，保证配对真实成立。

额外K16在2048和4096长度的GQA14/2参考与因果测试通过。数值/非有限/运行时错误将终止队列并保留失败；未达到质量成本门槛不自动扩长训。结果只适用于这个小模型LoRA和精度协议，不直接推广到全参数原生稀疏训练。
