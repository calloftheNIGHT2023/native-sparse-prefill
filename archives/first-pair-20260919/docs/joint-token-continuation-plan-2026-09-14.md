# 新A40迁移后：固定40至80轮延长检查

用户提供新Pod gcv07u2ggin9lk并要求继续试验。已读取新机A40、48GB，旧/workspace数据已由用户迁移；/opt运行环境需要重建。沿用总500美元、首阶段30美元额度，实际账单仍待核实，不新租更多卡。

目的只判断先前三组40轮结果是否主要是学习延迟。64维组在38/39/40轮验证准确率29.375/41.525/57.450%，不能据40轮终点称为永远学不会。原40轮结果完整保留，门槛不重写。

三组exact_r16_shadow、learned_r16、learned_r64全部从各自第40轮最终checkpoint接续到80轮；保持模型/索引器、AdamW两套状态、cosine T_max100、当前学习率、训练数据顺序、每批32和RNG。每组再12520主干与12520索引器更新，共37560主干及37560索引器更新，额外76800000输入tokens/4800000答案。不能仅延长落后组并与旧40轮对照称等预算胜出。

先检查完整文件SHA及三组旧测试预测15360个答案，0科学更新；CPU单线程断点前后技术检查与CUDA前向/梯度检查单列。旧PyTorch2.4.1+cu124/Python3.11为目标运行环境，不直接复制损坏的旧venv。跨设备并行浮点归约不保证逐位重现，必须报告新设备与环境。

新的1024序列MQAR，seed2026091481，运行前生成并审计，排除与旧train/dev/fresh、冻结容量fresh、旧joint fresh等整行重复；预先构造源值交换。新测试只在三组全部续训完成后使用，不在续训期间评测旧fresh或用它挑停止轮数。开发集每轮可见，无早停，无调参。

主要结果是80轮新题和交换准确率、首次开发>=99%轮数、40至80轮学习曲线，以及每组恢复源SHA。若80轮两个学习组达到新题和交换>=99%且距精确对照各不超过1个百分点，说明这一配置主要表现为有限预算下的学习延迟；仍不自动得到独立种子或真实LLM结论。若仍未达标，也仅说明80轮内未过门槛。

整体训练运行最多45分钟，各组12分钟。所有新增结果放results/joint-token-router-epoch80-v0，原40轮结果不改动。每轮保存可恢复checkpoint，逐步日志/UTC时间轴及新鲜测试文件/哈希保留。运行结束会回传本机核验。完成这项后不凭常规冷启动现象直接扩到大模型；仍需和MSA/KSA等前作划清贡献。
