# 同学习率与未适配底座：任务级事后诊断

在已观察的96道题上补齐密集/稀疏训练 × 峰值学习率0.0003/0.001的四格对照，仅用种子2026091560、已有256步完整断点。旧比较使用各方法在语言建模校准损失上选出的学习率，仍保留为最佳校准配置比较；本轮相同学习率比较只用于分离配置因素，不能用较弱密集配置替代原密集基线来宣布等价。

另增加原始密集预训练底座，分别以密集和K16运行。在密集训练断点通过原校准重放后，完全移除LoRA包装、恢复校验过的冻结原始线性层；不更新参数，不称“未训练模型”，不把它的来源断点哈希误称为已加载适配权重。此参照用于识别这轮适配前后任务变化。

全部6条件均评短文/无文/8K/16K，每种96题；共2304条计分预测，另18次热身、24个校准窗口前向。原密集低学习率和稀疏高学习率条件须逐题与归档的全部四选项logit及预测一致，容许误差1e-6；原校准NLL同门槛。任一失败停止，不放宽阈值。每个新权重源的结果哈希和checkpoint哈希已冻结到provenance/task-quality-factorial-protocol.json。

冻结后只执行一次，控制器900秒、每任务180秒上限，无训练更新。模型、数据、训练源码和环境沿用并核验。完整报告全部六格、配对题目差值；只有一个种子，数据已观察，区间仅描述且未经多重比较校正。新选方案如要确认，必须冻结方案、预设可接受差距并用新题；不凭未显著判等价。0.5B LoRA、明确标记TARGET的自建任务，不能外推为原生全参数训练或通用长上下文能力。

学习率对语言建模损失的历史诊断已经完成。本次新增的是现存权重的任务成绩，不重新训练旧学习率实验、不把学习率当原创贡献。它最多缩小下一步需要验证的假设范围，不能单独证明内部因果机制。
