# 80轮权重的选路必要性诊断（0训练步）

在读取本项结果前固定本计划。此前三组80轮已通过原任务门槛，不修改原结论与测试文件。现在只做事后描述性干预，检查长度64、4对KV、top8含2个局部位置的任务，是否必须依赖当前问题的内容选择。

使用已保存的新题seed2026091481：1024序列/4096答案及1024源值交换答案。三个80轮主干/索引器全冻结，CPU执行，不调用优化器。原模型与索引器权重SHA在前后核验。

每组测试6个条件：原路由；将索引器分数在同批样本间循环错配1位；固定位置优先保留1/3/5/7四个源值位置（再按位置升序补足）；固定位置优先保留0/2/4/6四个源键位置；优先前6个远处位置；优先最近8位置。全部因果、每query最多8条边、保留自身/前一位置，所有条件仍由原主QK在选中位置做内容注意力。固定值位置控制显式利用当前生成器布局，属于任务专用诊断，不是可推广方法。错配控制改变中间激活及后续路由；下降不能单独定位因果根源。

记录每层的监督查询数、命中对应源值比例、同时覆盖全部4个源值比例、选择边数和所有预测。原路由必须重现已归档GPU预测。固定位置控制若也>=99%，说明此任务上的高分不能单独作为“必须按问题动态检索”的证据；不证明训练得到的模型内部只用了位置捷径。

这一诊断不宣称原创：Zoology算法1公开把KV放在开头；ACL2025《Revealing and Mitigating the Local Pattern Shortcuts of Mamba》已经改变MQAR位置模式。先核验当前测试是否足够区分方法，再决定更多训练；MSA预热/KSA主任务梯度是既有基线，不能换名作为贡献。

输出results/router-necessity-audit-v0，保留计划、源码、数据/父checkpoint哈希与预测。旧训练文件不写入。预算：科学优化更新0、新GPU训练0。
