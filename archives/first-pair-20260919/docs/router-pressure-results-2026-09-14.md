# KV16压力测试结果

完整注意力对照在80轮内未过开发集门槛，已按冻结计划跳过三组稀疏训练。本批不构成稀疏方法之间的有效比较。

## 运行设置与结果

将旧任务从4对KV提升为16对，长度128；主干445952参数、2层、宽度128、单头，训练数据10000序列，开发1000序列。初始化seed123，训练80或40轮由dense第40轮开发集一次性决定。没有根据测试集调整预算、挑选checkpoint或修改门槛。

| 方法 | 实际轮数 | 最终训练准确率（eval） | 开发准确率 | 独立新题 | 交换前文数值 |
|---|---:|---:|---:|---:|---:|
| 完整因果注意力 | 80 | 42.6912% | 7.3250% | 7.1594% | 7.1289% |
| 预热4轮＋选中位置KL | 未运行 | — | — | — | — |
| KSA式任务损失训练索引器 | 未运行 | — | — | — | — |
| 固定保留前6个源值 | 未运行 | — | — | — | — |

dense第40轮开发准确率7.1750%，按事前99%门槛确定公共预算80轮。最终测试含1024序列/16384答案；交换题1024答案，只改前文两个源值，问题保持不变。测试只在全部获准拟合结束后运行。训练准确率为事后在eval模式计算，不能和带dropout的逐步训练指标直接等同。

若完整对照没有学会，只能说当前小模型、数据量与优化设置没有建立一个有效阳性对照；不能归因为稀疏性，因为dense没有删任何注意力边。它也不证明任务不可学习，或16对KV在该架构下必然无法泛化。训练/开发差距是诊断线索，不足以唯一定位原因。

## 主计划与证据边界

冻结计划docs/router-pressure-plan-2026-09-14.md及原始结果中的preregistered-plan.md相同SHA。两个学习索引器是前作机制的token级适配；固定位置是知道合成布局的对照。增加记录数本身不是原创benchmark，也不证明必须学习动态检索。前作边界沿用literature/router-recipes-overlap-2026-09-14.md，本批没有新增文献检索结论。

源数据在准备时完成标签/交换/去重检查，本轮再逐行复核；文件SHA为81e618abed621564c90b553462a2765d166c1a90d8c121d16a84433565189c29。旧Pydantic config.json未输出num_kv_pairs子类字段，使用的是已冻结且核验为16对的张量，不会据缺失字段再生成数据。源数据与旧manifest保持原样。

## 用时及计算量

| 方法 | 训练步骤累计秒 | 开发评估秒 | 每轮保存秒 | 完整拟合分钟 |
|---|---:|---:|---:|---:|
| 完整因果注意力 | 400.04 | 6.51 | 5.90 | 7.07 |

整批墙钟7.16分钟；新增25,040主干更新、0索引器更新、102,400,000输入tokens、12,800,000监督答案。所有运行同一A40，但当前小型参考实现的时间不能当作长上下文稀疏内核加速证据。

| 方法 | 主QK槽位 | 索引槽位 | 教师分布槽位 |
|---|---:|---:|---:|
| 完整因果注意力 | 26,214,400,000 | 0 | 0 |

槽位是逻辑张量元素数（含因果padding），不是FLOPs或额外QK重复计算；教师分布会复用主分数。总500美元及原首阶段30美元上限不变，实际费率/累计账单未知，未租新卡。


候选值诊断：

完整因果注意力的预测有96.3196%属于前文16个候选值，但答对率见上表。即使能输出前文某个值，也不意味着已学会问题与正确记录的对应关系；这是本批输出的描述，不是唯一机制解释。

## 核验和日志

本机与云端各8项CPU测试通过，CUDA四条件前向及有限梯度检查通过，准备0优化更新。49个结果文件SHA、源码/计划一致性、连续更新计数、初始化、优化器及调度器状态均核验。CPU重算17408个测试/交换预测与GPU逐项一致，另复核16000个开发答案，事后计算160000个训练答案。核验0优化更新。

0步选择覆盖诊断完成16384答案；固定掩码双输入可达性诊断未执行，因为固定掩码训练按门槛被跳过。此补充属于事后描述和正确性核验，不修改成功标准；源值进入选择集合不等于因果必要性。

逐步日志、每轮指标、训练权重及dense40/80节点、优化器/RNG状态、代码、数据审计和计划均保存于results/router-pressure-v0。CPU审计及曲线results/router-pressure-analysis-v0；诊断results/router-pressure-diagnostics-v0。完整时间轴TIMELINE.md。

## 决定

先解决KV16完整注意力的学习/泛化问题，再恢复稀疏配方比较；不据本次结果扩大模型或宣称稀疏方法失败。

尚未确认可以支撑论文的新贡献，没有真实文本或Qwen4结果。训练结束不代表Pod停止计费。
