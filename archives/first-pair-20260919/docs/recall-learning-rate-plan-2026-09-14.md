# 核对公开默认学习率：本轮最后一个参数对照

监督密度对照已完成且未通过，前驱绑定结构对照的3500步开发正确率约40.8%，仍将按4000步结束。此处固定一个额外CPU对照，完成后本轮不再扩展参数尝试。

Zoology固定提交1ad20d193b6113cae1e8f3c655c300d7b4b3f4bb的config.py第137行默认learning_rate=1e-3。当前使用3e-4，已在实现审计中记录。本次只改变峰值学习率，保留原来的warmup/余弦比例；不是执行原论文完整训练配方。

参照recall-supervision-last-v0：同一普通GPTNeoX全局注意力、初始化seed41、输入题族/答案变体、4查询输入但只监督最后一题、4000更新、每步16条、同一留出集。**不使用前驱绑定结构。** 唯一训练超参数变化为3e-4到1e-3。其余两组保留，不复用训练权重。

CPU墙钟上限900秒。固定最终开发与测试均80%门槛，最后一步评测，不按测试选择检查点。该对照是在看过此前开发诊断后追加；同一留出集已反复用于探索，后续若发现正信号必须另做未参与调试的新题族核验，不能称作预注册确认。

若仍失败，本轮不再扫学习率、增步数或租卡。若通过，先核验配对数据/初始化及查询替换行为，再记录为当前单种子、小任务的优化敏感性，不作为新算法贡献。
