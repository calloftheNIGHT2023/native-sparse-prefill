# 下一轮：现有检查点的固定中点复核

目的：确认稀疏模型训练前半程与后半程的任务成绩变化。新分析发现K32零步18.75%到128步32.81%，K48零步25%到128步32.03%；平均成绩有改善，但置信区间较宽，不能称已证明等价或持续收敛。

## 固定设计

- 模型：既有密集、K32、K48各两个种子，均评测第64步。六份断点的参数、优化器步数与哈希已在本机核验。
- 数据：原64篇开发文章，保留长文本与无原文两个条件，每模型128次，共768次题目计分。
- 验证：三种注意力各取种子0的既有128步模型，在四个目标位置各取一篇文章、两个条件，额外24次旧logit重放。三种模式都通过才进入64步评测；校准值重放阈值1e-6。
- 输出：统一展示0/64/128步；0和128步复用已审计结果。不用这组旧题选择最佳断点或声称独立泛化。
- 成本：0优化器更新；792次题目计分（含24次实现核验），另有每作业4个校准窗口前向。评测估计15–20分钟，整体硬上限25分钟，不含环境恢复。
- 失败处理：任意旧结果重放、校准、源文件或数据哈希不符立即停止；保留日志。今天不安排新训练。

## 当前实际状态

全部脚本完成语法检查，确认评测程序没有backward或optimizer.step调用，LoRA模块与原实验相同；六份零步检查点的96个B矩阵均为零，支持复用相同初始函数基线。没有运行新的GPU前向，不能把静态检查当作CUDA重放通过。

原Pod已Stop，等待新的SSH。恢复后先检查环境，按协议完成重放，再串行评测。跨GPU或软件版本变化不能直接绕过环境/数值检查。

协议provenance/midpoint64-protocol-v0.json；入口scripts/run_midpoint64_stage.py；审计scripts/report_midpoint64.py；发射包exports/midpoint64-launch-v0.tar.gz。父模型与数据留在原/workspace，若迁移新Pod需从已核验的expanded76/gentle32k证据包及模型缓存恢复。
