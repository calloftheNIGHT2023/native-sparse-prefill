# 两张独立 A6000 的 D/E 执行变更

用户提供第二张卡并明确用于并行。研究配方和科学比较不变：第一张继续现有 D，第二张从共同随机主干初始化训练 E；不切换 DDP、不加载工程预飞权重，不更改模型、数据、批次、词数时钟、优化器或评测安排。

第一张 REDACTED_POD_ID，A6000，driver580.159.04，计算$0.53/h，保守含存储$0.81/h。第二张REDACTED_POD_ID，A6000，driver570.195.03，计算$0.53/h，20GB容器与1000GB卷，按$0.68/h保守记账。第二张当前公开容量46068MiB，与第一张49140MiB不同；不能仅凭型号相同就视为环境等同。两组独立记硬件和时间，不把异机吞吐差用作稀疏加速证据。

第二张先迁移并逐SHA验证146个冻结文件，安装同版本软件；运行原tiny数值检查和E的12更新工程重放。原阈值不放宽；与第一张保存的E原始事件、共同主干/索引器初始化、数据顺序、损失和梯度核对后才允许全新E科学训练。新卡预飞上限$10，计入该Pod总额度；已完成第一张预飞不重复。

必须先撤销第一张父控制器的排队E，且保留D子进程、超时保护和科学协议。计划将父控制器下一组启动门槛所指向的顶层协议原子移到不可覆盖的调度交接目录，保留原字节/SHA；正在运行的D使用单独dense-protocol.json，保持原样。仅在源码和mock验证通过、撤销前后D PID/starttime一致且存活、旧E尚未运行时才签发解除调度收据。旧父未来在D完成后出现预期文件门槛错误，记作行政调度撤销，不记作D训练失败；D的原始summary、checkpoint和计数照常保留。不得恢复顶层原路径，否则会重新开放E。

单组最多72小时；第二Pod最多$75，包含部署、工程试跑和空转估算，两个Pod合并继续受原$150首对额度约束。第一张D按原72小时上限至多约$58.32，加部署；第二张额度$75仍留整体余量。报价墙钟是估算而非发票；重叠的预飞/训练费用不能重复相加。

执行结果以当前连接、解除调度收据、各Pod科学stage和UTC时间轴为准。两组结束后另行冻结完整dev评测，当前48窗面板不代替主终点。当前只是有界D/E先导，不新增方法或论文结论。
