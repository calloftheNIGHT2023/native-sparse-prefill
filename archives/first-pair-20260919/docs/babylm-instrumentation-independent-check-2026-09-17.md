# 阶段快照与梯度记录：独立 CPU 验证

2026-09-17T22:13:52.732161Z—22:14:04.530230Z。仅验证新增训练器记录行为，不重跑历史套件，不使用真实 BabyLM 训练数据、预训练权重、97M 候选模型或 GPU。

结果：5 项定向测试首次全部通过。可复核的逐项结果、源码 SHA256 与原始日志位于 `results/babylm-instrumentation-cpu-v0.json`，完整合成输入、checkpoint、阶段权重、故意损坏文件与异常记录位于其中 `fixtures` 指向的独立工程目录。主实现文件未由验证者修改。

## 快照检查

合成流每窗 7 词，冻结阈值为 5、6、14、22，词上限 22。第一个更新实际完成 7 词：5/6 两阈值共用一份权重，并明确记录实际 7 词、24 输入 token、23 loss token。暂停并恢复后，第二、三个更新实际到达 14、21 词；下一完整窗会超出预算，因此终点是 21 词，未将其标成已达到 22 词。

恢复前后的第一份权重 SHA 和修改时间均不变；多个阈值、阶段终点与重复恢复不重复保存同一点权重。最终 receipt 明示停止原因与实际词数。model-only 文件不含 optimizer 或 RNG 状态，不被当作精确续跑 checkpoint。故意损坏历史阶段权重后，恢复被拒绝。

独立检查了 JSON 与 Torch 文件的不可覆盖发布：已有文件保持逐字节不变，冲突失败的临时载荷保留以便审计。无静默覆盖。

## 梯度与更新的独立参照

独立参照将实际梯度展平后转换 float64 计算 L2 范数，并使用普通 PyTorch 全局裁剪和 AdamW 完成一次更新；没有调用训练器的 norm helper 来生成期望值。D/E 的共同参数初始化、输入和优化器设置与被测更新一致。

|设置|主干梯度范数|indexer 梯度范数|全局范数|裁剪系数|更新后参数最大差|
|---|---:|---:|---:|---:|---:|
|D|9.0398943165|0|9.0398943165|0.0022124150|0|
|E|8.8881890640|9.2772345465|12.8478397277|0.0015566818|0|

裁剪确实生效，新增记录未改变本次参数更新。日志的辅助权重、实际应用权重及加权辅助损失与独立前向一致。上述范数是 combined objective 的裁剪前梯度，不是对 LM 和 KL 的独立梯度归因。

纯张量边缘检查采用主干梯度 (3,4) 和索引梯度 (12)，独立期望范数分别为 5、12、13。空参数组和全部 `grad=None` 返回 0，原梯度缓冲区不变。非法或重复/乱序阈值在模型工作前拒绝。

## 评测接通与实际调用账目

在既有 D 的一步更新后，通过真实 tiny 模型执行一次合成开发窗口评测，确认 `eval_position_diagnostics=true` 经训练入口传入评测器。11 个有效目标进入对应位置分桶；位置诊断复用同一次前向 logits，没有额外模型前向或 optimizer 更新。

本次真实 full-model 计数为 **7 forward / 6 backward / 6 optimizer 更新**：4 次引擎更新、2 次独立 oracle 更新、1 次额外评测前向。所有人为异常和边缘检查保留。科学训练更新、真实 BabyLM 训练 token、GPU 调用均为 0。

这份结果只说明新增记账和保存实现通过有限的 CPU 定向检查，不提供语言学习差距、质量非劣或稀疏训练加速证据。
