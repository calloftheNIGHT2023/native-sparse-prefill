# 公开基线长度128检查：运行前固定计划

本轮在已通过的Zoology basic基线上，从头训练128长度模型；不启动GPU。总500美元、首阶段30美元的既有预算不变。本轮只使用本机CPU。

## 允许的配置变化

训练和验证序列长度64改128；绝对位置嵌入表也必须64改128，以覆盖输入。由此参数数目从437760增至445952，随机数消耗和其他参数初始值也可能改变，不能宣称与64长度模型逐位配对。词表256、4组键值、128宽/2层/1头、训练10000条/验证1000条、batch32、seed123、AdamW学习率1e-3/weight decay0.1、每轮余弦调度和最多100轮保持不变。数据power_a=0.01保持不变；长度增加不是强制所有查询远移。

保留原作者验证准确率严格超过99%才提前停止的规则。CPU时间上限3600秒，在轮边界检查；不根据新题结果改变停止规则或追加配置。

## 预先固定评测与停止

训练结束使用种子2026091425生成1000条128长度新序列，4000个答案。该种子与64长度评测一致，可共享键值模板，但128输入不参与训练、验证或停止判断；不是跨长度完全独立的重复试验。核验训练/验证/新题整行不重合、标签来自前方记录。

通过标准：原验证停止门槛达到，且独立新题准确率至少99%。另报告按序列bootstrap区间和各距离组（1—8、9—16、17—32、33—64、65—127）的准确率及样本数；分组结果不用于挑选模型。

对同一批新题，只将最后查询键改成另一前方记录的键，确认能否跟随查询给出新的正确答案。该检查可能产生重复查询键，属于探索性干预；不作为多训练种子证据。

无论成功或失败，保存源码/数据/配置/环境、逐步日志、每轮曲线、最终权重/优化器、异常原因与UTC时间轴。失败不自动租卡或扩大到256；成功后再设计更远位置的查找条件。当前没有新稀疏方法贡献，本轮是扩大测试条件前的可学性检查。

## 训练期间登记的补充只读检查（2026-09-14T04:18:38.3898868Z）

在最终新题分数产生前，补充固定的源值对调检查：每条新题取最后查询，将该键的前方源值与下一条记录的源值交换，保持查询与所有填充位置不变；两种正确答案必须不同。使用同一检查点和1000条新题，另报距离超过64的子集。此检查只用于诊断是否依赖前方绑定，不改变训练、停止或通过门槛，也不当作独立重复。脚本scripts/probe-zoology-source-values.py。

