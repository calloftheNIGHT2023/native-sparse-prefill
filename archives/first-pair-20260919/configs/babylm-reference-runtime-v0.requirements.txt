# Local reference versions. Torch accelerator build must be chosen for the
# actual GPU/driver, then validated; this file is not a CUDA validation claim.
torch==2.10.0
numpy==2.4.2
transformers==4.57.6
tokenizers==0.22.2
safetensors==0.8.0
huggingface-hub==0.36.2
