# 冻结主干：低成本索引器容量检查（开跑前固定）

用户允许稍微增加开销并要求继续。总500美元上限不变，本批仍控制在原首阶段30美元内；先使用已有空闲A40，不新租卡、不增加总额。

主干为上一轮native组最终权重，437760参数、2层、1头、64长度。全部主干参数冻结。索引器直接读取每层注意力输入，独立仿射Q/K投影到16/32/64维；不带RoPE、不做块池化，因此只是低秩token级容量参照，不冒充QSA/MSA原始架构。所选token仍8个，强制最近2个。推理主注意力使用真实gather的8个KV，不计算全量主QK；gather复制和小任务的开销仍需如实记录，不宣称高效内核。

四个拟合条件：rank16/32/64随机初始化（索引器seed2026091460）；rank32从主Q/K的仿射双线性评分矩阵截断SVD初始化。另有rank128直接复制主Q/K的0更新等价性对照，以及原模型精确评分对照。SVD使用已训练主干的信息，只用于静态容量/优化诊断，不能声称从头训练方法。

训练数据仅用已有训练集前4096序列，缓存原始native路由下每层输入和完整因果主QK的softmax分布。索引器拟合完整因果KL目标，不使用任务答案来训练。固定40轮、batch32、AdamW lr0.001/weight_decay0，无调度或早停；两个层共享一个优化器，每个条件5120步，共20480索引器更新，主干更新0。

已有验证集只作开发监测。新独立评测1024序列，生成seed2026091451，检查不与原训练/验证/旧测试整行重复，只在拟合完成后评测。报告最终任务准确率、NLL、改变源值的正确率，以及只替换单层/两层同时替换的诊断。两层共同替换时，后层输入可能偏离缓存分布，不能把这类退化直接解释为索引器维度不足。

若某个不超过64维的索引器在新题任务与源值调换两项都距精确评分模型不超过1个百分点，可进入同架构的原生联合训练定位。否则先检查单层替换、静态拟合与输入分布变化，不直接扩训。比较只是定位基线；MSA/KSA已有索引器学习/预热，不以这些共有内容申报新贡献。

先跑CPU正确性与A40数值等价性检查，再启动本批。整批设置20分钟内部墙钟上限，逐步日志与定期检查点持久化；保存源码、依赖、数据哈希、主干不变核验、时间轴。上一批启动器/文本尾部异常不能忽略，本批直接记录Python成功/异常状态，并用结构化日志与最终权重复核，GPU进程上限不等于停止Pod计费。
