# C1：完成已筛选分数尺度配方的一轮训练，再评完整开发集

用户在 B2 完成报告后明确要求“继续”。本协议只授权续完同一条训练轨迹及固定终点完整 dev，不授权学习率扫描、多种子、W 或自动重试。选择继续发生在已看到 B2 的 500 步结果之后，属于开发阶段筛选；不是独立确认或预先注册的最终测试。

## 问题与固定比较

B2 将索引器分数的统一尺度从 1 改成 `1/sqrt(128)`，固定 500 步面板 PPL 从 102.0274 降至 100.2316。此次检查这项小幅改善是否在原定完整一轮终点仍然存在。正的统一尺度在固定权重下不改变 top-k 排名；它改变辅助优化的分数温度和梯度。暂不声称新方法或已复现完整 Qwen 的冷启动机制。

沿用原随机初始化、全参数、从第一步稀疏的 B2 轨迹。主干峰值 LR `3e-4`、索引器峰值 LR `1e-3`、原 warmup/cosine 词数进度、AdamW 状态、种子、数据排列、批量和模型结构均不改变。前 500 步不重跑，不把剩余 913 步重新设为一轮退火。

唯一预定终点是 1413 更新、22598 训练窗口、10001709 词、16325414 输入 token、16302816 监督 token。新增训练为 913 更新、14598 F/B、6372311 词、10435590 输入 token、10420992 监督 token。新增过程面板为 750/1000/1250/1413 各 48 窗，共 192 eval F；保留历史 0/250/500 面板，不重评第 500 步。

## 恢复与证据保真

冻结训练引擎要求恢复到相同 run_dir、相同完整协议与日志。因此在新的 C1 目录建立明确标记的行政分支，不修改 B2 原件：

- B2 events 的完整字节前缀原样保留；历史快照及其 SHA 原样保留。
- 新分支仅重绑定输出目录、预算/墙钟及相应协议与快照来源链；生成父子 SHA 收据。原历史事件仍引用原证据，不能改写成新实验历史。
- 模型、优化器、全部 RNG、数据 cursor、计数、学习率、最后评测位置与已用 elapsed 逐字段/张量验证不变。重绑定不能重置费用或计时。
- tiny CPU 等价测试检验正常恢复与行政分支恢复的科学状态一致；工程计数单列。仍不保证任意 GPU 中断前后的数学轨迹在所有平台逐位一致。
- 同一 Pod/MIG/驱动/runtime/科学源码必须保持；已完成的 GPU 数值预飞不重复。身份、显存可用性、共享锁及进程隔离重新核验。

## 看分前固定的评测和解释

1. 训练末 100 更新固定为 1314–1413。报告按监督 token 加权的纯 LM NLL、其指数 PPL，以及逐更新 PPL 的算术平均；辅助 KL 不混入语言损失。
2. 唯一最终 1413 步模型评原完整 dev：18792 窗、17437534 输入 token、17418742 监督 token、10420962 词。沿用六个来源、原窗口顺序和位置桶，无梯度、无参数更新；不挑最佳中间快照。
3. 分别对照已完成的原 E 和 D。同完整 dev 的旧 E NLL/PPL 为 3.983205566/53.688861889，D 为 3.938299515/51.331239024。报告有符号差值、PPL 相对差、六来源及位置差异，不将不显著当作等价。
4. 下降的训练 loss、48 窗面板、全零路由率与完整 dev 是不同证据。只有完整 dev 全部审计通过后才报告最终分数。全零率必须注明所用真实 query 分母。
5. 若完整 dev 改善，下一建议是同预算从头 W 局部窗口对照，以检验学习式远距路由是否有必要；若改善消失，则保留负结果，先定位尺度与动态训练的关系，不靠挑终点挽救。

该 dev 已参与研究开发，不能当作未接触的确认集。单种子不能估计训练随机性的方差。当前完整分数参考实现没有真正稀疏加速证据，不能据本批 runtime 宣称省时，也不能外推完整 Qwen 或大规模预训练。

## 有限运行与费用

同一张 48GB MIG 顺序执行续训及评测，无并发抢卡。续训监督等待上限 10800 秒（含准备），完整 dev 最多 3600 秒，整个专属进程组外部硬截止 14400 秒（TERM 后 15 秒 KILL，合计四小时）。训练引擎保留 B2 累计 elapsed，其内部累计墙钟预算与本批新增墙钟分开记录。

本新批上限 6 美元，原优化周期上限 20 美元。当前 GPU 实价 1.09 美元/小时，含存储保守 1.40 美元/小时；四小时执行最多按 5.60 美元估算，另留 0.40 美元部署余量。部署、等待和训练以同一个 Pod UTC 区间核算，子阶段费用不与该区间重复相加。历史未观测闲置和实际账单仍未知。

协议/来源不匹配、非有限数、计数不符或超限即停止队列、保存失败，不自动延长或重试。成功后备份并逐 SHA 核验，独立复算终点指标再汇报；自动监控正常时安静，最终通知后暂停。不会自动购买、停卡、删除 Pod 或对外发布。
