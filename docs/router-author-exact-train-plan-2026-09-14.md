# 精确主QK top8从零训练控制：追加批次执行前计划

在三种既有稀疏配方尚未全部完成、尚未读取其终点测试之前固定。此项是补齐归因所需的控制，不是新算法。先完成前一批并保留全部原始结果，再串行运行，避免同GPU同时训练影响时间。

唯一新增条件exact_native：保持作者配置128宽/2层1头/Identity/8192词表、长度256/KV16；相同未更新seed123主干、同一100000训练/3000开发、batch256固定顺序、AdamW lr0.01/wd0.1/cosine T_max64、float32/TF32关闭。两层从第一步即通过完整主QK选top8（含自身及前一位置），再在被选集合softmax，不另训索引器，不预热、不用教师。使用既有ScheduledAttention的native路径。主分数仍全量计算，此控制只隔离聚合稀疏和低秩索引器学习，不能声称高效kernel或GPU加速。

固定19轮/7429主更新/486400000输入tokens/30400000监督答案，不提前停止、不按结果加轮或调学习率。模型选择和拟合只使用原开发集；先前测试不用于新增超参或挑checkpoint。若前批执行技术失败先处理失败，不并发或默默重跑。单项15分钟/外层17分钟保护，现有A40、不租卡；总500美元/原首阶段30美元边界不变，实际账单未知。

开始前在当前作者模型上核验精确mask与独立参考的前向/梯度/因果性，0优化更新；不另做优化测速。正式训练从原始初始权重开始。每轮保存原子checkpoint及4/8/16/19节点、优化器/scheduler/RNG；每步损失/梯度/LR/UTC/样本数/时间及分数账本；开发每轮记录。

本批拟合完成后，统一用新的1024序列seed2026091642、源值交换1024答案、噪声seed2026091643（只替换零填充）重评估五个最终检查点：原完整对照、已固定的三种配方、exact_native。不基于这组结果决定本批停止或选择权重。保持完整对照新题/交换>=99%的有效性门槛，以及各候选开发/新题/交换>=99%且距完整对照不超过1个百分点的通过门槛。噪声仍仅诊断。

若exact_native通过而rank16配方不通过，只能说本固定配置和预算里额外索引机制是一个待查因素；索引器结构、损失、优化器、主干梯度路径等仍同时变化，不能独断某一个原因。若exact_native也不通过，仍不能用19轮一次失败断言全程稀疏不可行。冻结已训练dense的top8诊断与从零训练必须分别报告。

0技术优化步，单种子、合成前缀布局、主LR来自完整对照开发选择。数据及参考检查点SHA、源码/计划、完整预测和时间轴均保存；结束回传CPU逐项重算预测、SHA/步数核验和双端备份。不自动扩展学习率、种子、模型或新卡。
