# Conditional confirmation allocation

2026-09-14 UTC, while Stage A is still running and before either alternate-learning-rate fit or fresh test is run.

Clarify the unused third-fit slot in falsification-v0. If the 64-epoch continuation fails, run seed123 from the original initialization at the already specified author learning rate 0.002154434690031882. If that reaches the final development threshold, allocate the third fit to initialization/training seed124 at the same learning rate and otherwise identical settings. This confirmation tests whether a counterexample survives a changed initialization; it is not a new paired speed comparison. If the alternate learning rate fails, the third fit instead uses the next author grid learning rate 0.00046415888336127773 at seed123. No test-driven selection, no simultaneous fits, same 1800-second individual cap and three-fit/90-minute total cap.

The original Stage-A-success branch remains unchanged (seed124 native and dense, both learning rate0.01). The new test is generated only after all selected fits complete. A finite grid failure remains a finite grid failure, not an impossibility proof.
