# W 原轮训练结束后的 ARM / GB300 迁移

用户提供新主机并要求迁移。保留原 W 1413 更新的完整训练，不创建新候选，不在新平台重做或恢复反向训练。旧卡的 eval-template 已原子移到 handoff 日志目录，原训练未改；训练最终审计通过后，旧外层因找不到该模板退出属于行政调度撤销。

新主机为 aarch64 / NVIDIA GB300。保留原绝对项目根 `/workspace/native-sparse-prefill/babylm-optimization-20260920-v0`，另建 ARM Python 环境，版本固定为 torch2.10.0+cu128、numpy2.4.2、transformers4.57.6、tokenizers0.22.2、safetensors0.8.0、huggingface-hub0.36.2。原科学源码、数据与检查点逐 SHA 校验；不新增 FLA/FlashAttention 改变执行路径，不修改全局驱动或其他项目环境。

数据执行集为原两个 window manifests、7 个 artifacts、12 个 token streams，共21文件。保留原 W 所有日志、全 checkpoint、model-only快照、最终收据、完整训练协议和来源信息。初次慢速本机上传已终止且部分文件保留，改用服务器直传；一次性签名私钥只存在接收进程内存，旧卡只临时授权一个固定、只读、已校验归档的发送命令，完成后移除该公钥授权。密码和私钥不进入项目、归档、日志或输出。

新卡 GPU 还有其他已有进程，保持原样。每个迁移验证或评测持有本项目 GPU 锁，核对物理 UUID；不声称独占 GPU，不把新旧设备或共享负载的耗时混为稀疏加速证据。

执行顺序固定：

1. 软件/GDN源 SHA、数据/模型 SHA 与目标 GPU 运行环境只读核验。
2. 新卡运行原 W tiny 全模型数值门槛一次，仍用已有 CPU/CUDA 与梯度阈值。外层240秒以内，所有工程F/B/更新单独计数，失败保留，不自动重跑或放宽。
3. 唯一 final1413 权重按原固定48窗在新卡重放：总 NLL 绝对差不超过1e-6，每窗绝对差不超过1e-5；窗顺序、所有token分母和注意力计数精确一致。新入口已通过独立审阅，纠正并保留了初稿误用相对容差的问题，未执行过错误版本。最多48工程评测F，0反向/更新，外层600秒内。
4. 只有数值门槛通过后，才使用冻结 W full-dev wrapper 在新目录评完整18792窗、17418742监督token。原W末100步固定1314–1413，由旧卡完整日志独立复算。完整dev尚未得到前不能当成最终质量或非劣证据。

迁移不自动重启监控，不对外发布，不停止、删除或购买云实例。旧卡费用按原实际1.09/h、含存储保守1.40/h分UTC区间核算；新卡单价已向用户询问，未知金额不能填0或套用旧单价。新环境安装限20分钟、tiny限4分钟、重放限10分钟；完整评测仍最多3300秒并保留原W绝对截止2026-09-21T05:59:32.739689Z。付费执行是否满足已有金额边界必须在记录中明确区分已知费用与待核实费用，不保证未经核实的总额。

完成后先核验原始证据并在本机备份，再报告新的连接、已完成的训练和新卡实测状态。不能把环境已安装、数据已传到或门槛已通过当成完整评测已完成。
