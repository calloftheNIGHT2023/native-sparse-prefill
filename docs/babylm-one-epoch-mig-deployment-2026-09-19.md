# 1 epoch 实验的 Blackwell MIG 执行补充

2026-09-19：用户提供新Pod REDACTED_CONNECTION_METADATA。连接成功，实际为PRO6000 Blackwell Server的1g.24gb MIG分区，CUDA可用总显存25,367,150,592字节，GPU计价经Pod API读取为$0.59/h；20GB容器盘、1000GB卷，沿用$0.81/h保守分配估计，不是实际总账单。

新执行协议为 configs/babylm-one-epoch-blackwell-mig-20260919-v2.json。完整继承v1的科学配方：架构、初始seed、数据、学习率、AdamW、分组裁剪、严格一轮、1413更新、末100定义及所有原科学源码SHA均保持。新增字段只有硬件身份、单卡D→E调度和执行适配来源；实际GPU报价从原A6000的0.53更新为当前0.59，不用跨硬件耗时宣称稀疏加速。

新GPU已在最终工程目录使用原始阈值数值套件通过7/7检查，9F/9B/3工程更新（其中GPU7F/7B、CPU2F/2B），不计入科学训练。环境为Python3.11、torch2.10.0+cu128、numpy2.4.2、transformers4.57.6；51个数据文件、原15个源码逐SHA匹配。

nvidia-smi对父物理GPU的进程和显存查询报告权限不足。MIG执行适配器明确记录不可见，不将其视为空队列；同时核验指定父UUID/driver、唯一MIG、CUDA driver v2 UUID、唯一CUDA设备、22–26GiB总显存且至少90%空闲，并在独立CUDA探针退出前后检查当前容器其他进程的GPU设备FD。MIG级flock防重复。本检查不声称可观察宿主机所有容器。

由有限顺序控制器运行D后E；D必须完整结束且末100审核通过才启动E，否则停止，不自动重试。每组6h协作上限/6h10m外层上限、$12分配，合计$24；全序列外层上限12h30m。旧周期监控仍暂停。本序列只产生这一对固定实验，不追加预热/窗口/其他seed，不把工程修改宣称新方法。

数据、源码、协议和日志分别留存，最终结果仍是单种子早期训练诊断；训练尾段PPL与开发面板分别记录。新卡实际吞吐需在启动后测量，旧A6000的3–4h估计不能作为新卡保证。

## 实际启动记录

最终执行协议为`configs/babylm-one-epoch-blackwell-mig-20260919-v3.json`，SHA256`cc7efbb5da17c8798cc55e6f002585ecb76f8b35875e03b522508e2f1d9a500d`。v2两次入口失败均在首次科学前向之前，旧输出未覆盖。v3只增加对已核实nginx模板服务的FD读取权限例外：固定child PID527/UID65534/parent526，父进程root、exe为/usr/sbin/nginx且无GPU FD。不可见的子FD仍记作未知；不是证明宿主机完全空闲。

19:43:45UTC启动D→E，D训练PID6660。19:50:40UTC已读到78/1413更新，1,248训练F/B、913,185输入token、911,937监督token；初始过程dev独立48F。原始日志中的协议hash使用规范化JSON，文件校验使用原始bytes，两种hash用途不同；额外本机日志审计已按原有规范化方式核对。没有改动训练或指标。

前78更新平均5.06s/update，故D全程暂估约2h，E尚未在本卡实测，不能给同等精度估计。总额$24是分配上限，GPU报价$0.59/h和$0.81/h含存储保守估计分开记录；从19:18:15UTC首次部署观测前的租用费用未知，当前记录不是发票或账户余额。

正在增加独立的有限进程安全守卫，以覆盖训练子进程start_new_session脱离控制器进程组这一终止边界。守卫只核验本轮PID/start_ticks/cwd/父子归属，不访问GPU、不启动训练、不做实验监控或扩展；周期automation保持PAUSED。其部署成功以logs中的启动/运行收据为准，不因文档说明就视为已经生效。

安全守卫于19:52:37UTC部署，19:53:27UTC已核验PID6933/monitoring、0训练信号，固定截止2026-09-20T08:13:45.353180UTC。7项mock通过，原科学源码及协议SHA均未改变。
