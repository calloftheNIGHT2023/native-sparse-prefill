# Stage B：固定特征诊断后的下一步（2026-09-20）

用户提供新 Pod 并要求继续。迁移原代码、数据和快照，原阈值的 tiny GPU 数值检查和48窗口跨卡重放已通过。没有重跑旧D/E或完整dev。

固定第137步模型产生的24窗口特征，只训练索引器；12窗拟合、12窗路由holdout，每个global层holdout368个固定查询。四臂均200次更新、相同初始化与AdamW；不是全模型预训练。完整原始备份/独立审计在当前状态中记录，以下是终点汇总读取。

| 条件 | 第4/8/12层全部候选非正比例 | 三层平均完整注意力质量覆盖 |
|---|---|---|
| 原LR与原尺度 |82.34% /95.65% /100%|38.12%|
| 只把LR降10倍 |0% /82.61% /98.10%|38.35%|
| 只把尺度改为1/sqrt(128) |55.43% /4.08% /0%|49.56%|
| LR与尺度都改 |0% /0% /0%|47.86%|

只降LR没有通过先前冻结的coverage/KL/退化联合筛选；只改尺度与联合臂通过。选择只改尺度，保留原索引器LR1e-3，不同时改多个因素。原方案B2已将出现温度/饱和证据后的单因素尺度试验列为候选。

这不是“问题已经解决”。第一层仍有55.43%的查询全部非正；将尺度恢复为1来评价，尺度臂的KL反而较大，因此不能声称原温度下拟合更优。动态选块coverage的变化涉及学到的排序变化，同权重的正尺度变换本身不改变topK。这个结果仍来自固定hidden与固定教师，不能推出语言质量。

下一步只做一次从随机初始化开始的全参数、step0稀疏500更新前缀，唯一科学改动是index_score_scale=1/sqrt(128)。保持原1413步/10,001,709词的一轮warmup+cosine调度、主干LR、indexerLR、数据顺序、参数初始化、裁剪和稀疏预算。记录第401–500步token加权纯LM NLL/PPL，以及0/250/500的固定48窗面板；500步不是完整一轮。对照原E同样前缀，禁止用跨卡时间比较宣称加速。

本次新Pod总上限$5，原优化周期$20；实卡报价$1.09/h、含存储保守$1.40/h。500步外层最多90分钟，协议内科学运行5300秒，结束后先备份审计并汇报，不自动加长或开新参数搜索。
