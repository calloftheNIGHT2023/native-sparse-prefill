# 作者高记录数完整对照：执行前计划

沿用现有A40，不租新卡。总500美元、原首阶段30美元上限未改变，实际累计账单未知。本轮是公开配置的阳性对照校准，无新方法贡献；之前KV16负结果保留，不与这批不同数据/模型的准确率当成单变量因果比较。

配置来源固定commit 1ad20d193b6113cae1e8f3c655c300d7b4b3f4bb的iclr24_zoology_figure2/configs.py，源码SHA074e53f43b2be0235c4e55547db3c9be3919323fc5edf1cf5a851c384b051262。直接运行已存源码得到配置对象，筛选attention、d_model128、(length256,KV16)。词表8192、state mixer Identity、2层/1头、无随机填充；100000训练/3000开发、batch256，AdamW wd0.1，cosine T_max64，最多64轮，开发准确率严格>99%时按作者规则提前停止（该轮scheduler尚未step）。保留完整子类字段，核对配置差异。

从作者学习率网格按事前顺序选0.002154434690031882、0.01；第一项若开发过99%，跳过第二项。否则第二项也最多64轮。两项从同一seed123主干初始化开始，同一固定数据顺序。两次拟合都结束或被跳过后才读终点测试，不据测试选下一学习率或挑checkpoint。两个未选择的更低学习率和其他宽度本轮不扫，因此失败不能代表作者完整网格失败。

训练/开发数据种子按作者prepare_data的np.RandomState(123)抽样规则生成；另冻结1024条新题seed2026091601及1024个源值交换答案。通过标准：开发>99%且新题/交换各>=99%。同时用seed2026091602给同一新题的0填充位置加随机tokens，保持前文KV、监督问题和标签不变；这是0步、事先登记的噪声诊断，不参与停止和通过门槛，不把干净题成功说成原噪声任务成功。

输出投影仅在labels!=-100的16个位置计算，backbone仍读完整256个位置、做完整因果注意力。选择依据仅为监督位置掩码；标签值只用于CE。这省掉不参与loss的词表投影，数学上与原始ignore_index CE等价；先在相同随机状态下核验logits/loss/所有梯度和共享embedding梯度，再做CUDA复核，不是新稀疏注意力方法。float32、TF32关闭，不宣称与原训练逐位相同。

准备8个一次性CUDA优化步用于时间/显存测量，明确不计入科学训练；所有正式权重从未更新的初始化开始。若估计64轮完整运行超过40分钟，则不启动科学训练，先保留测量讨论执行规模。单条件45分钟、整批90分钟墙钟保护。每轮原子保存最近完整checkpoint；8/16/32/64节点另存，含优化器/scheduler/RNG。中断留独立失败快照，禁止默默重复完整更新。

数据、标签、跨分割去重、初始化/配置/源码/计划SHA、每步UTC损失梯度/耗时/样本数/LR、每轮开发和checkpoint、终点预测/环境/manifest全部保存。回传本机逐项重算GPU预测，并保存报告、STATE和TIMELINE。若对照通过，只说明作者这一子配置可用；后续稀疏比较另固定共同预算。本轮不自动开展稀疏扫描。
