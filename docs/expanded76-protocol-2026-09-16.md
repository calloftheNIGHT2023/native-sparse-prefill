# 下一轮：增加训练文本后，成本与质量是否仍然接近

2026-09-16，时间轴采用UTC。云端控制器26858于03:38:58启动。

已完成的256步结果：同token稀疏节时17.56%，PPL高5.02%，长题37.50%对密集39.06%。这是描述性成本折中；64个复用问题、两颗种子尚不能证明统计等价。密集128的PPL优于密集256，不能忽略较早停训这个低成本参照。

本轮增加训练文本的覆盖：32个窗口扩大到76个，44个新增窗口来自同一固定WikiText训练流。两种方法均重新从原预训练权重初始化LoRA，共享每颗种子的初始参数、76窗口顺序和学习率。每条轨迹固定128更新，相当于约1.68遍数据。学习率沿用原来64步衰减到0.0001的计划，之后保持不变。

|项目|本轮固定安排|
|---|---|
|模型|Qwen2.5-0.5B，32K上下文，Q/K/V/O rank8 LoRA|
|注意力|密集Flash SDPA与K32 FlashMoBA，128-token块|
|精度|FP32主权重，BF16 autocast，FP32均值选块|
|训练|两种方法×原两颗种子×128步，共512科学更新|
|运行顺序|密集seed0、稀疏seed1、稀疏seed0、密集seed1|
|校准及存档|0/32/64/96/128步；另外保存第2步断点|
|正确性预检|每种方法6诊断更新，检查前反向、分块损失、因果性、断点恢复；K32额外核验真实稀疏选块及梯度|
|最终评测|4个固定终点+2个零更新模型，9个WikiText窗口和RACE64×3变体，共1152任务前向|
|作业上限|GPU控制器3600秒，每个作业最多650秒；失败保留，不自动重试|

主要比较本轮密集和稀疏的同token时间、PPL和任务成绩；记录端到端作业时间，但与纯训练循环节时分开。历史32窗口曲线作为描述性参考：本轮同时改变覆盖与样本顺序，不宣称只操纵一个因素的严格因果实验。

测试语料与64个RACE问题复用前阶段，明确属于探索性实验；不根据测试结果选学习率、训练长度或最佳断点。本轮4条新优化轨迹沿用已有两颗种子，不增加独立初始化种子数。仍不是Qwen4、从零训练或全参数原生稀疏预训练。

冻结协议：provenance/expanded76-protocol.json，SHA `52cb6a240ebc194acfb3dcc2a6fe76bcac349fafbd161f64f5b35a14fe12055d`。
启动包：exports/expanded76-launch-v0.tar.gz，SHA `f0c7a49a179acff5ddb3a99a11657dfbbac4602b2c70f23bfbff01c52982ec30`。

云状态logs/expanded76-queue-v0.json；本地收集logs/expanded76-local-collector-v0.json。完成后逐文件SHA核验并审计完整AdamW、scheduler、RNG、数据游标和任务预测；报告写入docs/expanded76-results-2026-09-16.md，并附所有作业时间轴。Pod未配置自动Stop。
