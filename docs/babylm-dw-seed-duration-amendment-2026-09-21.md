# D/W确认队列：依据新卡实测修正运行时限

用户继续训练并准备关本机的授权不变。原v0于07:25:40UTC启动，但新共享GB300首11更新处理126,248输入token，训练更新时间约125秒；按token量归一，整轮约4.5小时，原3小时训练界明显缺少余量。这个估算仅用于运行安排，不用于稀疏加速或质量筛选。

07:29:45UTC向已核验身份的D worker发送TERM，让引擎保存失败记录后父队列停止后续。v0原master、脚本、起止时间、全部提交/未完成窗口、模型证据和实际消耗均保留。失败快照不用于恢复，旧更新不改称工程或从账目中删除。

新v1保持原科学配置、seed20260921、数据顺序、模型、学习率、优化器、完整一轮、末100和完整dev相同。仅改变新输出目录、源码/协议身份、行政时间限：每组训练软限21,500秒/硬限21,600秒，每组dev7,100/7,200秒，队列58,200秒（16小时10分），无自动重试/延长/追加候选。两个训练都fresh开始，不复用v0权重。

此修正不改变旧运行记录，也不是按质量调整超参数。仍为未知费率、有限时间预算；记录本批实际秒数，旧v0时间单独计入累计消耗，不作美元上限已核实的承诺。6小时是每臂硬上限而非保证完成，共享负载可能变化；失败时保留证据并停止后续。

科学协议继续遵守 docs/babylm-dw-seed-confirmation-protocol-2026-09-21.md 中的数据、种子、比较与报告标准；本文件仅替代其时间配置。关机前仍须确认新v1实际训练更新已推进且脱离本机连接。
