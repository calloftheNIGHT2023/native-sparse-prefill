# W完整开发集补齐协议

用户在2026-09-21询问“不能补齐吗”，据此执行补齐剩余评测。旧3200秒作业已经终止并保持原样；本批是新的有界剩余窗口评测，不延长或重启旧进程、不恢复训练。

## 已固定的输入

- 模型：原W随机初始化全参数训练1413更新的唯一final1413，SHA `854b912cb76e889abd6c3fb7ef101c004b42576cc01425b6dde19033b790ab39`。不重新选择检查点。
- 原全dev协议：`configs/babylm-w-arm-migration-20260921-v0/full-dev.json`，SHA `0c5e625ba993a3a0b51b12d167fe64d5a63a364f5cf31d99f7ff98e5f49d7d4d`。
- 原前缀：连续0..12401，12,402窗，15,739,613输入、15,727,211监督token；windows SHA `c23183ba0f66ef70d359cb970561876a5023c652d90ab2328a1274eccf3dd959`，终态summary SHA `42e3f00f974f931f8793db287b0ed93ee4e4bcda7c2be1aac1eb4239b89ea990`。其超时和已消耗计算保留，前缀不重算、不改写。
- 新窗口：严格12402..18791，6,390窗，1,697,921输入、1,691,531监督token。不抽样、不丢弃零目标窗口、不选择有利来源。
- 继续使用同一GB300物理UUID、驱动595.58.03、FP32精度、线程数、GDN参考路径、局部窗口模型、tokenizer、数据清单、评分器和26项科学源码。15项tiny及48窗原阈值跨卡重放已通过，不重复预飞；实际runtime必须仍与冻结值一致。

## 单次执行边界

新结果目录 `results/babylm-w-arm-remainder-20260921-v0`，原件只读。只增加6,390次科学LM评测前向、0反向、0优化更新。新行政包装器只约束和记录子集评分，沿用原评分器及W模型工厂；默认仅静态验证，执行必须校验master SHA。

评分软限3200秒，外层最多3300秒（TERM在3285秒，15秒后KILL），新启动时写明UTC硬截止；失败不自动重试/追加窗口。所有进程处于本项目GPU锁内。主机其他已有任务保持不变，不声称GPU独占。

新卡单价尚未得到，金额不可估作0或套旧卡价格；本次明确记录最多55分钟新增占用和实际起止。用户授权补评，不进行任何购买或无限批次。旧53分钟评测、迁移工程及本次补评分别计数，合计实际成本，不删去超时记录。自动监控仍暂停；本轮只完成用户要求的补齐工作。

## 完整结果门槛

1. 原前缀与补评均有逐窗原始日志、冻结来源、准确执行计数及本机SHA备份。
2. 两段并集恰好0..18791，顺序唯一且无重叠；窗口元数据、每来源、位置桶和监督token分母与固定dev数据一致。
3. 并集总计18,792次评测前向、17,437,534输入、17,418,742监督token。总NLL按监督token加权，PPL为exp(总NLL)，不平均两个PPL。
4. 独立复算W完整分数并逐窗对齐D/E/F；保留零目标窗口，但其PPL不进入有目标token的均值。
5. 结果注明由同卡两段评分合成，完整dev已用于开发，不当作独立盲测。单种子不能证明统计优势/非劣，不从GB300/MIG或共享负载计时推出稀疏加速。

本批完成后先交付完整结果和证据，不先开启新训练或其他候选。
