# C2：恢复中断的同一轮训练，不新增科学候选

用户在中断报告后明确要求“接着做”。本批仅从C1已核验的完整第1000步检查点恢复到原终点1413，随后评原固定完整dev。自动监控保持PAUSED；有限队列内的硬截止和失败清理继续启用。

## 不变的科学比较

- 小型GDN/QSA混合模型，原随机初始化与全参数、第一步稀疏轨迹；index_score_scale=1/√128。不更改模型、数据、优化器、学习率或原1413步/10001709词调度。
- 保留原训练窗口顺序及模型、optimizer、Python/NumPy/torch/CUDA RNG、数据游标、累计计数。第1000步完整状态是唯一恢复点，不选择分数更好的快照。
- 完成一轮的末100步固定为1314–1413，分别计算纯LM token加权NLL、exp(NLL)和逐step PPL算术均值；辅助损失不混入PPL。
- 后续固定18792窗完整开发集，与原D/E相同数据和评分实现，17437534输入、17418742监督token。只有唯一final1413权重通过来源/计数审计才允许评测。
- 单seed、已用于探索的dev；不称统计非劣、加速或论文成功，不外推完整Qwen。

## 中断保全与实际计算量

原C1全1074步日志、旧锁、检查点与元数据只读保留。新目录 `results/babylm-stage-c2-scale-recovery-20260920-v0`，只把完整检查点所提交的前5471715字节日志复制到恢复分支；原后74条日志单独保留，不拼接到新轨迹。

从1000恢复需要413更新、6598训练前向/反向、2837782词、4681167输入token、4674569监督token。原1001–1074已执行的74更新、1184训练前向/反向、523716词、877457输入、876273监督token仍是实际支出。若本批完成，这74步不能通过回滚计数从成本里消失。

本批新过程面板仅1250和1413，共96评测前向；0–1000面板沿用原记录，不重复计为新训练。独立迁移重放48前向另记工程验证。整批科学新增LM前向6598+96+18792=25486，反向6598；tiny GPU工程验证另外9F/9B/3updates。未落盘在途计算未知，不记作零。

## 换卡验证与来源

同Pod重启后物理GPU/MIG变化，driver580.126.16、torch2.10.0+cu128及numpy2.4.2已读回。先验证MIG身份/空闲内存/容器可见进程/独占锁，再跑原7项tiny数值与梯度阈值和第1000步同48窗重放。重放总NLL绝对误差≤1e-6、逐窗≤1e-5，attention计数精确相同，不放宽门槛。

新recovery wrapper只在门槛通过后，将runtime身份和行政目录/预算等绑定到新分支；训练引擎与科学源码不改。旧runtime、完整来源SHA及每项行政变动在收据中保留。恢复是第二次明确记录的resume。模型/optimizer/RNG等非行政状态按类型和张量字节验证一致。不同GPU段的耗时分别记录，不用跨GPU段宣称加速。

## 时间和费用边界

- 迁移验证共最多1200秒（tiny600秒，重放600秒）；失败停止，不自动重试。
- 恢复训练含准备最多5400秒；完整dev最多3600秒；外层整队列10800秒硬截止，覆盖所有同进程组子任务。
- 实际GPU1.09美元/小时，含存储保守1.40美元/小时。队列最多4.20美元，连同迁移验证在本次6美元上限内，沿用优化周期20美元上限。既有观测费用、当前空闲和新执行按互不重叠UTC区间计算，不把训练子区间再加一次。
- 不自动扩种子、增加W、重试或延长截止；完成后待本机原始证据审计。不中止/删除Pod，不投稿，不恢复周期自动监控。

部署前的Windows命令行长度错误发生在SSH启动前，0远端执行/0模型调用；已改为stdin传输，失败收据保留。
