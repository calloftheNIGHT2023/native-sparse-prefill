# 作者高记录数完整对照结果

作者配置中的这一完整注意力子条件已通过开发、新题和源值交换门槛，建立了单种子阳性对照。

本轮直接读取固定commit 1ad20d193b6113cae1e8f3c655c300d7b4b3f4bb中iclr24_zoology_figure2配置，取attention/d_model128/长度256/KV16子集。词表8192、2层1头、Identity state mixer、无随机填充、100000训练/3000开发、batch256、AdamW wd0.1、最多64轮。主干1214720参数。选择顺序0.002154434690031882、0.01在运行前固定，首项开发>99%才跳过第二项，测试在全部获准拟合之后才运行。

| 学习率 | 轮数 | 开发 | 独立新题 | 源值交换 | 配对加噪声 |
|---|---:|---:|---:|---:|---:|
| 0.0021544347 | 64 | 7.8417% | 7.3242% | 6.6406% | 6.4941% |
| 0.01 | 19 | 99.9917% | 100.0000% | 100.0000% | 99.9756% |

未运行的第二项学习率：[]。更低两项学习率和其他宽度不在本批计划中。新题1024序列/16384答案，源值交换1024答案；加噪声是在相同新题的0填充位置替换随机tokens，前文KV和监督问题不变，不参与通过门槛。作者较大词表/长度/模型与前两批不同，不能把本表与旧7%左右的分数当作单变量改进对比。

干净题与噪声题使用同一检查点；若干净对照未通过，噪声下降不能被包装为已建立的泛化机制。

| 学习率 | 加噪声准确率下降 | 按序列配对bootstrap 95%区间 |
|---|---:|---|
| 0.0021544347 | 0.830个百分点 | [0.519, 1.154]个百分点 |
| 0.01 | 0.024个百分点 | [0.006, 0.049]个百分点 |

区间使用5000次序列重采样，只描述当前测试样本的不确定性，不代表跨训练种子波动。

## 注意力描述性诊断

| 权重 | 层 | 源值→前一个键 | 问题→对应源值 | 问题→对应源键 | 问题→全部16源值 |
|---|---:|---:|---:|---:|---:|
| initialization | 1 | 0.1056 | 0.0160 | 0.0161 | 0.2567 |
| initialization | 2 | 0.1055 | 0.0160 | 0.0159 | 0.2567 |
| lr1 | 1 | 0.0024 | 0.0509 | 0.0011 | 0.8526 |
| lr1 | 2 | 0.3300 | 0.0033 | 0.0021 | 0.0510 |
| lr2 | 1 | 0.5298 | 0.0302 | 0.0182 | 0.4310 |
| lr2 | 2 | 0.0871 | 0.4605 | 0.0005 | 0.4684 |

这些是eval模式的平均注意力质量，不是因果必要性或失败原因证明。初始化和最终权重各16384个监督位置，0更新；数据和代码见results/router-author-attention-v0。

## 数值正确性、预算与保存

输出层仅投影16个监督位置，backbone仍对完整256位置做稠密因果注意力。CPU/云端各4项配置、前向/损失/全梯度、因果性、干预测试通过；CUDA全梯度最大差2.1234e-7。共享输入/输出embedding保持绑定。这是损失等价的计算省略，不是新的稀疏注意力算法，也没有Qwen4或真实文本结果。

一次性预检8个优化步单独记录；正式训练全部从未更新初始化开始。新增科学主干更新32,453、索引器0、输入tokens2,124,800,000、监督答案132,800,000。输入tokens含固定数据的重复暴露。批次墙钟14.36分钟。总500美元及原首阶段30美元边界未改；没有新租卡，实际费率/累计账单未知。

60个结果文件SHA核验通过；数据标签/跨分割去重、作者完整子类配置、初始化、连续步数、优化器及scheduler状态核验。作者在达到开发门槛当轮不再执行scheduler.step，这一差异已计入审计。67584个GPU测试/交换/加噪声预测CPU逐项重现，另核对96000个开发答案；分析优化更新0。

代码版本只读核查：2024模型文件的默认Transformer路径在当前attention/config模块下，与当前model.py初始化SHA及两个样本的完整输出相同。此项只隔离model.py，不是完整2024依赖栈复现。证据docs/router-author-version-audit-2026-09-14.md。运行中未修改科学源代码。

结果results/router-author-control-v0；CPU核验及曲线results/router-author-analysis-v0；注意力诊断results/router-author-attention-v0。数据/初始化/逐步日志/每轮指标/最终权重及8、16、32、64可用节点、优化器/RNG、源码/配置/冻结计划完整保存。时间轴TIMELINE.md；训练前后状态记录logs/control-state.json。

根progress在首项拟合结束前仍显示generating_data，这是进度显示缺陷；真实进度由lr1/2的progress和events记录，训练完成后根进度已更新。第一次本机状态写入使用默认GBK读UTF-8失败，显式指定编码后恢复，未影响科学训练。

## 下一步

先根据噪声配对结果界定有效任务范围，再登记相同数据/模型/预算下的既有稀疏配方比较及独立种子确认。

这轮是公开基线校准，尚不构成新的论文贡献。旧KV4已有配方成功和旧KV16配置失败各自保留，不能混成同条件对比。GPU训练结束不会自动停止Pod计费。

来源：[作者配置](https://github.com/HazyResearch/zoology/blob/1ad20d193b6113cae1e8f3c655c300d7b4b3f4bb/zoology/experiments/paper_configs/iclr24_zoology_figure2/configs.py)。
