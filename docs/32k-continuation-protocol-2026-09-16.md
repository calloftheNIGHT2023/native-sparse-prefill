# 32K续训：固定训练量与近似等时比较

协议在2026-09-16T02:06:32.975302+00:00冻结，SHA见provenance/32k-continuation-protocol.json；云控制器02:07:00.749152Z启动，PID21740。

目标是测量成本与可接受质量的折中，不要求稀疏质量超过密集。

- 从上一轮已选好的密集/K32、两种子、LR峰值0.001的第64步完整断点续训到128步；共256个新更新，不是四条新的独立随机初始化。
- 原AdamW状态、调度器、随机状态和数据游标全部继承，原学习率调度尾部固定0.0001；不将调度长度改成128，不重新预热。启动时重放原校准NLL，误差须≤1e-6。
- 数据仍为同32个32K训练窗口，固定顺序循环。每条续训增加2,097,152目标曝光，不是新增独特文本。
- 第80/96/112/128步保存校准与完整断点。稀疏还逐步保存跨越对应密集64步历史训练时间前的所有断点；等时点取最后一个累计训练用时不超过预算的完整更新，越界的第一步仍保留并计入实际实验成本。
- 密集64步预算分别236.6216502673924秒、236.33576856181026秒。历史父训练时间加本轮续训计时，并非同时新跑的严格同墙钟实验；不能把同一步数说成绝对同耗时。完整训练循环计时含搬运、前反向、优化器，加载、校准、保存和服务空闲另列。
- 固定评测四个128步点、两个稀疏等时点、两个密集64步点和两个种子0零更新基线，共10模型。训练长度和断点选择不看最终评测。
- WikiText采用与训练相同的双换行格式，此测试已观察过，本轮属探索复用。另冻结64篇新的RACE test文章，排除旧pilot/formal及上一轮64篇文章，每篇一个问题，短/无原文/32K三个变体，共1920次计分前向。预训练污染未知；人工长上下文拼接不是官方RACE。
- 运行顺序K32种子0、密集种子1、密集种子0、K32种子1。四条训练完成后才打开最终测试与新题。控制器最多2400秒，失败保留证据、不自动重试。

模型仍是密集预训练Qwen2.5-0.5B的rank8 LoRA，不是Qwen4或从零原生全参数稀疏预训练。两种子与64题不能单独证明统计等价、通用长文保持或论文原创性。
