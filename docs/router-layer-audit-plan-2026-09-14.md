# 两层干预后的事后定位（0步）

在初次两层替换诊断之后追加，不伪称事先预注册。已观察两层固定值位置约78%、两层错配约64–69%；两层同时改动会破坏上游表示，不能据此认为第二层必然需要动态索引。

现在对三个80轮checkpoint分别只改第0层或只改第1层；每层用fixed_values或other_row_scores两种干预，另层维持原路由。共12条件/61440答案。仍用已有seed2026091481的新题与交换题；不调用优化器、不写权重，不增加云端训练。固定值位置按公开生成器指定，不读取答案用于构造掩码。

若只改某一层保持高分，可认为该特定替代在这些冻结权重/此分布下足够；不能据此推广到从头训练，也不能说原模型内部唯一依赖或不依赖某机制。数据分布与路径干预仍可能产生相互作用。单种子、固定4对KV的范围不变。
