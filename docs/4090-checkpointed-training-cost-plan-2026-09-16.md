# 下一步草案：当前4090上的训练主体成本检查

状态：准备方案，尚未启动，不是训练结果。

动机：整模型缓存prefill的SD在4090慢约38.22%；这不能替代反向传播测量。旧32K训练峰值约39–40GiB，当前24GiB无法按原设置重跑。

固定一个已验证的128步密集训练检查点（seed2026091660），保持所有参数不变，用相同训练窗口比较密集与K32。两组均使用相同的非重入梯度检查点、FP32参数/BF16 autocast、256-token分块冻结输出头反传和梯度裁剪。先按原生注意力复现4个校准NLL到1e-6。

实现门槛：2048-token短输入上分别比较开启/关闭梯度检查点，两模式共4次前反向，损失差小于0.001、梯度相对L2小于0.02（沿用既有chunked-gradient门槛）；没有通过就不跑32K。

成本主体：32K训练窗口0、1、2，两模式各1次完整预热，三轮每个模式重复2次，交替方法顺序，共14次32K前反向。记录前向、分块头反传/骨干反向、裁剪总耗时、峰值显存、损失、梯度有限性与重复性。计时和梯度核对分开，避免CPU下载污染GPU计算时间。0优化器更新，不产生新科学训练检查点，不用质量评测题训练。

此轮总计18次梯度计算；仅证明当前硬件和省显存设置下的训练主体计算成本，不包含优化器步骤、数据加载、正式训练收敛或论文级成本优势。任何失败原样保留。建议单作业600秒硬上限（GPU时间约0.124美元，不含准备/空闲/存储），源代码实现后再冻结正式协议。
