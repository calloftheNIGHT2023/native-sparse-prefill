# 已有检查点的质量—训练成本曲线

用户再次明确：节省成本且效果足够接近即可，不要求超过密集。此次按完整质量—成本取舍推进，不沿用原NLL差0.03或路由修复门槛来否定所有取舍点，也不把较低分数自动称为失败。

## 执行范围

- 原密集LR3e-4、K16 LR1e-3，各两个种子。每条轨迹评第2/64/128/256步，16个已有检查点，0新优化更新。
- 每个检查点评同96题的8K与16K版本，共16×192=3072计分前向。第256步768次是对已有结果重放；其他检查点提供新轨迹信息。两个版本是同题不同输入，不是192道独立题。
- 每个检查点评已有29个保留语料窗口，以及原校准窗口。29窗口已在此前公开过结果，本轮属于旧数据探索，不冒充新留出验证。单独记录NLL窗口前向，不混入3072任务计分次数。
- 训练成本使用原逐步日志中从第1步到该检查点的同步训练循环累计时间。既有256步学习率日程保持不变，早停点不是重新优化过的短训练配方。数据曝光为step×16384；不把重复窗口当更多独立语料。
- 同步核验原归档检查点SHA、参数名称、身份、step、数据游标、保存的逐步记录。64/128/256步校准与当时记录重放，2步没有历史校准值，只报告新计算值；256步另与旧29窗口NLL及全部任务logit核对。

## 报告方式

给出所有检查点的训练秒数、PPL、8K/16K正确率及配对区间。分别展示同更新数比较、相对密集256步参照、以及已有点之间的成本—质量关系；不能省略更早的密集检查点来制造稀疏优势。

列出PPL代价3%/5%/10%，任务代价3/5/10个百分点等描述性容忍范围，不把其中某档冒充用户已经指定的唯一合格线，也不以“不显著”判等价。若某点更便宜且效果接近，记为后续确认候选；选点发生在旧数据上，不直接升级为独立验证或论文贡献。若尚有较大差距，也按实际取舍呈现，不要求稀疏超过密集。

不从本轮推理计时推导新训练加速；完整搜索、安装、评测和空闲费用也不同于训练循环时间。原15条AMP训练及历史开销保留，当前没有重新训练费用，但云卡评测和空闲仍有实际计费。

总作业上限1800秒，单条轨迹450秒；四个加载作业、每个4检查点。超过上限停止并归档部分记录，未完成不补成完成。两种子检查点顺序反向；所有16点都报告，不因中途成绩调整步数、学习率或题集。不会自动启动新训练或新题确认。
