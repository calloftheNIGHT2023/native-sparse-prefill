# 在通过密集能力门槛的任务上检查已有训练轨迹

固定词值任务已核验：密集零步短题64/64，无原文16/64；32K为54/64，32对中23对两个事实版本都答对。短题只是4个独特的事实输入重复测量，无原文只有1个独特输入，不当成64个独立问题；32K有32组不同背景，按背景组配对分析。

现在评测密集/K32的固定0/64/128步轨迹。零步各方法只有一个功能基线；64/128步各两颗既有种子。密集零步沿用同4090刚核验的192条原始预测，其余9个状态全部新测，合计1728次前向、0训练更新。

输入、四个候选词、模板、背景和事实位置完全冻结，不调整能力门槛，不筛样本。每个状态都完整运行短题、无原文和32K共192条，即使该状态不通过能力门槛也保留全部结果。主结果是32K准确率和“两份事实都答对”的配对成功率，完整展示0/64/128，不能挑最高点。

四段原校准NLL必须逐检查点按1e-6重放；模型和源文件按原SHA校验。区间按32组背景配对bootstrap，共同保留事实版本和种子。此任务已被能力筛查使用，因此属于机制开发诊断，不是独立测试；即使结果接近，也不能单凭这项简单词检索认定论文成立或否定旧阅读理解上的落后。

上限2700秒，每模型240秒，实验GPU费上限约0.555美元，不含准备/空闲/存储。全部原始分数、源码快照、检查点引用、UTC时间轴和失败归档均保存。若发现后续训练降低了事实读取能力，下一步才考虑有明确保留目标的训练干预，并先核查已有方法。
