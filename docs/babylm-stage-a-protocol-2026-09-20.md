# 已启动：现有检查点评测与路由干预，阶段 A

用户已回复“可以”，接受优化方案。首轮优化共享预算上限 $20；本次只执行其中的已有模型评测/诊断，不启动任何训练，不恢复周期自动监控。

## 冻结入口与边界

- 主协议：`configs/babylm-optimization-stage-a-20260920-v0/master.json`。
- SHA256：`d83fd0d75667a1c3399579b3775516ca73253b40ca60774a635d4ace3420e368`。
- 16 项单卡串行任务，源文件、检查点、原训练协议、窗口清单、数据、依赖与 FP32 数值设置均锁定。
- 调度：D final48 重放 → E final48 重放 → E 在 update137/693/1413 的 learned/prefix/local/random，各固定48窗 → D/E 最终完整开发集。
- 先跑短诊断是为尽早得到机制信息；全部16项在看分前冻结，本批内部不按结果挑配置、窗口或快照，也不据中途结果开始训练。
- 原模型与训练核心源码无修改；使用已保存的 model-only 快照做严格 state_dict 加载，没有加载 optimizer 或恢复训练。
- 同一已核身份的 24GB Blackwell MIG；沿用现有互斥锁和 MIG 身份/内存/进程描述符检查，未重跑 tiny 训练预飞。

## 重放门槛

全局 token 加权 NLL 绝对误差至多 1e-6；各窗口平均 NLL 绝对误差至多 1e-5；窗口顺序、输入/监督 token、词计数和所有原注意力计数必须完全一致。原每窗口 token-loss reduction 与 LM mean reduction 存在浮点求和差异，因此两个数值阈值分别固定。任何失败保留结果并退出，不自动放宽阈值。

最终 learned 诊断也必须通过原 E final48 的同样重放，验证观察工具没有改变预测路径。干预组采用相同64完整块预算、相同因果尾块；随机模板仅由固定 seed、层名和 segment 长度/块几何决定，不依赖运行顺序，不污染全局 RNG。

## 评测口径

完整 dev 每组18,792窗口、17,437,534输入token、17,418,742监督token、10,420,962保守词暴露。逐窗保存，最终按监督token加权 LM NLL，PPL=exp(NLL)，辅助KL排除；来源及查询位置分桶复用同一次模型前向的 logits。

48窗面板每组77,755输入token、77,707监督token。整个固定队列全部成功时为38,256次 LM 评测前向，0反向、0训练更新。诊断模块额外的 indexer、主 QKV、完整因果 attention 和四策略选块调用另行计数，不隐藏为“免费”算子。

coverage 是同一模型、同一层输入下，完整因果 attention 的被保留 token 概率质量之和。四种路由的 coverage 在该输入上比较；整模型换路由后，后续层输入可能改变，不能跨两次干预前向声称同一 hidden。零分率给精确分层/位置/监督范围的分母，重点关注可见完整块大于64的有效截断查询。

固定权重的 mask 干预不能替代从头训练 W；完整 dev 被用作后续开发证据，不能再当未看过的最终确认集。不得从单seed/48窗/这种参考实现的耗时宣布等价、加速或复现大型原架构。

## 限额、保存与计数

启动于2026-09-20 03:46:21 UTC。工作总限14,280秒；外层timeout在14,380秒发送TERM，再给20秒后KILL，整个进程组硬限14,400秒。两组重放每项600秒，诊断每项600秒，完整dev每项5400秒，并受更严格的剩余总时限约束。评测器不派生模型工作子进程；退出码、完成状态、全部窗口 ID/计数均核验后才接续。

沿用本Pod已核GPU价$0.59/h、含存储保守计价$0.81/h；四小时执行上限$3.24。部署、准备和任务结束后空闲另计且归入共享$20，不能把重叠用时相加；这不是账户余额或发票。Pod仍由租赁计费，外层脚本退出不等于停Pod。

每窗口JSONL flush+fsync。单次采集只备份日志、协议、摘要，裁掉可能正在写入的不完整最后一行并记录长度，逐文件SHA验证；不重复下载模型、不覆盖旧证据。采集脚本 `scripts/collect_babylm_optimization_stage_a_v0.py` 没有循环或自动调度。

本机验证：runner 8项基础及1项路由集成通过，新增10 tiny LM前向、0B/更新；诊断模块9项测试通过，成功测试含9次独立attention组件前向（不当作完整LM前向）；控制器5项逻辑测试通过、0模型调用。95,391,000参数真实D checkpoint仅严格加载核验、0前向。首次诊断测试的argsort参数兼容失败及首次本机部署命令的SyntaxError单独留档；部署SyntaxError发生在任何传输/模型调用之前。工程诊断与科学训练不混账。

启动/授权：`logs/babylm-optimization-stage-a-20260920-v0-launch.json`、`logs/babylm-optimization-authorization-current.json`。后续当前进度登记在 `logs/babylm-optimization-stage-a-current.json`。
