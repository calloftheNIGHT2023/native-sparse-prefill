# 更温和稀疏设置：先解除当前显卡的内核资源限制

## 为什么继续这一步

256篇新文章的完整评测已核验：同128步训练时，K32长文准确率31.25%，密集44.73%，差-13.48个百分点，文章配对95%区间[-18.75,-8.01]个百分点；短文差-1.76个百分点。训练节时17.35%成立，但当前长文质量差距不能称为近似相同。完整结果见fresh256-results-2026-09-16.md。

因此下一步考察保留更多块的K48/K64。此前失败发生在CUDA内核启动，不是模型效果验证；必须先解决实现问题，才能判断它们的时间与质量折中。

## 已完成的实现修复与验证

官方FlashMoBA固定提交39d9ac043b271d046a2181a9991e99a26b67bca1的K64选择模板，用128查询一组处理，计算需要106496字节动态共享内存，超过这张RTX6000 Ada每块101376字节的上限。K48也走K64模板。

独立候选模块把该分支的查询组降为64，编译时static_assert核验需求57344字节。评分、选择逻辑和epilogue沿用原实现；目前只支持BF16、head64、causal、K33到64。命名空间与动态库单独保存，运行时只在指定Python进程里分流K>32的选块调用；原注意力、排序与K<=32仍用原扩展。

固定CUTLASS子模块提交a2439551c765c5393aebe557ee75d3a0412d2211，已通过上游API核对。使用当前CUDA12.4工具链编译独立选块模块，耗时86.21秒；原完整FlashMoBA扩展保持不变，未替换包。候选库SHA `bd64a9456360a268d66d88aee35a73e9506ff5acdacec2fa6c5dfdfac3f044bf`。

首轮构建在编译前因PATH找不到已经安装的ninja退出，没有运行数学检查。第二轮只给构建子进程补上现有虚拟环境bin路径后成功。失败与修复两轮原始包均保留、下载并逐文件核验。

|检查|K48|K64|
|---|---:|---:|
|相对FP32评分的选块regret|0|0|
|前向相对L2误差|0.2279%|0.2285%|
|Q梯度相对L2误差|0.2895%|0.2910%|
|K梯度相对L2误差|0.3325%|0.3318%|
|V梯度相对L2误差|0.3299%|0.3308%|

检查固定8704token、68块、14查询头/2KV头，比较末16个查询的显式FP32所选块因果注意力输出及对应Q/K/V梯度；K48/K64均是真正稀疏。阈值沿用既定前向2%、梯度3%，没有为这次结果放宽。路由索引作为离散量，不求其导数。该检查不是对所有长度/形状的形式化证明。

本工程阶段0优化更新、0任务预测。成功证据包27文件SHA `23694a87c772d8382196b0437a7defb98003f455298b112dbbdfbb21b2ceb134`；首次失败24文件SHA `405c95af963f5a615fd52c732dbe9b49145bca1f032f2ffd551551c1bea40e91`。

## 正在执行完整更新速度筛选

密集/K32/K48/K64，三轮交换顺序；每个作业从同一密集64步断点恢复完整状态，2步预热、4步计时。先做原密集校准重放和当前档位数学检查，再跑32K整模型前向、反向、裁剪、AdamW与scheduler。模型是Qwen2.5-0.5B、rank8 LoRA。

三轮均快于对应密集且所有检查通过，才列为质量训练候选。完成当前速度实验不自动宣称质量保持，也不将12个计时作业当成独立训练种子。最多72诊断更新，0科学更新、0任务预测；控制器最多900秒，失败日志保留，后续该档位跳过。

协议provenance/m64-density-cost-protocol.json，SHA `703d66c6df317e529f44e7d210ab8f566a67111f1c475a1eede183c363214d0d`。云控制器29877，本地采集37856；状态logs/m64-density-cost-queue-v0.json及logs/m64-density-cost-local-collector-v0.json。完成后自动下载审计并生成docs/m64-density-cost-results-2026-09-16.md。

这一步是支持实验的内核适配，不宣称新稀疏学习方法或论文贡献。K48/K64的训练后质量仍未验证。
