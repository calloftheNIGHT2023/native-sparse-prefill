# 256本书完整恢复对照：启动前计划

上一批9组均核验。64步稀疏恢复相对密集原始提升2.832pp，98.333%区间[1.465,4.346]；128步提升3.564pp，区间[2.051,5.322]。但密集64步相对稀疏128步差-2.832pp，区间[-4.346,-1.465]，也通过原5pp容差，并且已有训练流程成本低约44%。因此不能把128步稀疏宣称为同质量最低成本。

本轮检验密集模型同样做Q/K恢复后是否也达到接近基座的长程检索，以及恢复对稀疏与密集的收益是否不同。采用已有方法作为对照：QK-Restore、稀疏训练后密集评测都不是本项目原创，相关工作边界见qk-restore-overlap-2026-09-16.md及sparse-training-overlap-addendum-2026-09-17.md。无新方法、无新增训练。

缺失条件为两个训练种子×64/128步×密集恢复/稀疏不恢复，共8组，每组原1029条题。保留之前密集不恢复与稀疏恢复8组和基座1组，不重复运行。所有评测均密集注意力。原检查点、数据、问题、答案词、数值门槛保持不变。新增8232任务预测、32校准NLL，0优化器更新。累计预测从56150到64382，历史失败及两条迁移重放不删减。

每个检查点先重放4条原校准NLL，最大误差<=1e-6；否则停止。恢复将48个Q/K LoRA B矩阵置零，其他参数必须一致，独立保存恢复后适配器并核验全部张量与摘要。逐条保存logits、预测及标签，两个父归档和所有新归档文件哈希复核。

固定4项比较：64/128步各一项“稀疏恢复减密集恢复”，各一项“稀疏恢复收益减密集恢复收益”。每书先平均4个事实变体、再平均两个种子；配对重采样256书20000次，种子2026091711。4项各98.75%双侧区间，量化本批条件统计不确定性；前两项仅下界>-5pp判非劣，后两项报告交互及区间，不当等价检验。不能把未显著视为相同。此前3项比较不改写、不删除。

此计划在看过上一批结果后制定，同一批书已暴露，属于自适应诊断，不能视为独立确认。不同书ID不能保证作者/版本独立。基座99.71%、稀疏恢复约99%有明显天花板，交互接近零不等于不存在机制。没有自然QA、新模板、新种子或更大模型支持。

控制器单卡串行，GPU须空闲、旧采集器已完成、启动回执只能创建一次。预计约96分钟，硬上限7200秒，单组1150秒。以此前4090费率$0.74/h估计计算上限$1.48，新Pod实际费率尚未核实，不包括准备、闲置、存储，也不是账单。沿用$500原预算及用户后续有限放宽授权，本轮不上规模。评测耗时不得用来证明训练省时，训练计时只引用之前同GPU冻结实验。

完成后按原始证据审计，保留任何失败。若恢复让密集也接近基座，则缩小“稀疏特有保护”解释，保留已有成本差；若存在交互，则仍需在新数据或新种子上独立检验，不直接宣布机制或论文成立。不自动循环训练。
