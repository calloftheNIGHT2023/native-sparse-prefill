# 32K延长到256步的预定规则与协议

意图冻结UTC：2026-09-16T02:21:06.513247+00:00。触发判断UTC：2026-09-16T02:23:56.564942+00:00；完整协议冻结UTC：2026-09-16T02:23:56.656035+00:00。

第96到128步，两个稀疏种子的校准NLL分别下降0.00435869、0.00466254，平均0.00451061；均为正且平均超过预定0.002门槛，触发接续。该门槛在128步新RACE评测前固定，判断仅读取训练/校准结果。

待128步全部报告与归档完成、GPU无其它计算进程后，依次续训密集种子0、稀疏种子1、稀疏种子0、密集种子1。所有轨迹128→256步，共512新更新，继续原0.0001学习率尾段、AdamW、RNG和同32窗口顺序。到256步为8遍数据，不是新增独特语料。

每160/192/224/256步保存校准和完整断点。额外保存稀疏累计训练用时不超过相应密集128步用时的最后一个完整更新；训练到越界下一步以确定边界，仍如实计入本轮开销。两密集128步预算是472.91435秒、472.82467秒。

固定评测四个256步点、两个稀疏等时点、两个密集128步点，1536次RACE预测；沿用前一阶段64题及WikiText测试，明确属于复用探索，不是新的独立确认。原零更新基线仍保留于父报告。所有任务评测排在全部训练完成后，不依任务结果选停止点。

扩展控制器最多3600秒，无自动失败重试；完整包保存所有断点、逐步时间/损失/学习率日志、协议和源码。未来统计脚本scripts/report_32k_extension.py会核验父状态接续、时限边界、原始包及全部任务预测；脚本已准备，扩展结果当前尚未完成。

时间边界：扩展门槛制定时，第一种子的部分校准曲线已可见；门槛冻结早于128步新RACE评测，且扩展阶段只读校准决定接续。因此属于校准驱动的自适应探索，不声称在所有校准结果出现前完成预注册。
