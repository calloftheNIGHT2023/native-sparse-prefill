# 固定权重交换注意力：分开读取路径与参数适应

事实词轨迹已完整核验。密集0/64/128步的32K准确率84.38%/82.81%/82.03%；K32为48.44%/51.56%/46.09%。短题均100%。当前128步稀疏训练没有在这一冻结任务中修复初始长程读取差距；这不等于所有稀疏方法不可行。

下一步固定128步权重，完整做2×2对照：密集训练权重、K32训练权重，各自以密集和K32注意力评测。每种权重有两个既有种子。四个原生组合结果已有并核验，本批只运行四个交叉组合，共4×192=768次计分前向，0更新。

每个作业先保持其训练时的注意力路径，检查4段校准NLL与原日志误差不超过1e-6；通过后才切换评测路径。权重、输入、候选词、逐题顺序和数值环境固定，记录训练k和评测k，不能把切换后的数值当原生重放。

分析完整四组合：固定稀疏权重改成密集是否恢复；固定密集权重改成稀疏是否下降；同一注意力下两套权重是否不同。按32背景组配对bootstrap，保留事实两版与两个种子。所有组合和区间全报告，不选有利种子或位置。

此前RACE任务做过密集回切未修复的诊断，不能隐去。本次新增信息是直接事实词任务已显示明确事实依赖，可检验那些RACE负结果是否能迁移到有效的上下文读取测量。仍仅是机制诊断，不是新方法、独立泛化确认或加速证据。

上限1500秒，每作业300秒；本卡GPU费上限约0.3083美元，不含设置、空闲和存储。失败保留，不自动重跑或放宽校准阈值。
