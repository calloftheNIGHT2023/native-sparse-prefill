# 作者对照的0步注意力诊断

第一学习率训练中、终点测试产生前登记；不改主计划、优化器、预算、权重选择或通过门槛。

在未训练初始化和每项最终权重上，使用同一1024条干净新题，只做eval前向。统计两层中：源值位置对其前一个源键位置的平均注意力；监督问题对对应源键/源值的注意力；问题对全部16个源值的总注意力。记录均值与答案/源记录数。

统计只描述各层的信息聚合方式，不能单凭某条边权重推断因果必要性，不能证明学习失败的唯一原因，也不算新方法。此诊断读取监督位置来定位问题，不读取答案值来改变模型行为。所有操作0优化更新。
