# 已学会模型的精确选路诊断（0更新）

本诊断在稀疏三组运行期间登记，使用已经冻结的作者完整对照lr2第19轮权重，只在全部拟合结束后读取本批新测试题。条件为：原始完整注意力、两层均保留精确主QK分数选出的top8（含自身和前一位置）、仅第二层改为上述top8。没有其他学习率、排名、稀疏率或checkpoint选择。

通过把每层主Wqkv的Q/K权重复制进rank128索引器，使用项目既有FrozenRouter实现相同分数的选择。必须先核验复制后完整分数与主QK一致。此实现为了知道精确top8仍计算全部128维QK分数，故只有聚合路径被截断，不能称为高效稀疏算法或从零稀疏训练成功。

统一报告1024新题序列/16384答案、1024源值交换答案、16384配对噪声答案。原始完整预测要与本批dense-reference逐项相同；每种干预前后主干权重SHA不变。若top8保住能力，只说明这个已学会的模型可以沿更少的聚合边执行，并不能说明rank16索引器可学、从零训练可行或路由必要。若仅第二层可行，则两层表示变化是尚需区分的因素。

0主/索引更新。输出results/router-author-exact-mask-v0，代码scripts/audit_router_author_exact_mask.py。与从零19轮的三组训练结论严格分别报告。
