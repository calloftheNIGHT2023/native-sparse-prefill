# 新初始化D/W单轮配对确认（2026-09-21）

用户明确要求“继续，开始训练我就关机”。本批在已迁移服务器上独立运行，自动监控仍暂停。有限队列完成后退出，不自动扩种子、改方法或重试，不依赖本机保持开机。

问题：旧初始化中固定局部窗口W的完整开发集PPL为50.071871，密集D为51.331239。更换初始化后，这个方向是否仍然出现？这不是学习路由的新方法试验，也不是证明完整Qwen架构或端到端成本改善。

唯一新初始化种子20260921；原种子20260917保留为探索结果。D/W两组共同新主干的146个参数tensor、95,391,000参数必须逐SHA相等，并区别旧种子；预先在当前GB300 CPU构造核验，0前向/反向/更新。数据顺序种子固定20260919，indexer_seed字段沿用20260918但两组均无indexer。固定数据顺序使本批只研究初始化扰动，不声称跨数据顺序重复。

科学设置沿原一轮：同tokenizer、训练窗口、文档边界、95.391M混合模型，9层GDN与3层global；D全密集，W从第1步使用最近64个完整块（块长4）加原因果尾部。学习率3e-4、1,000,000词warmup后cosine退火至0.1倍，AdamW betas=(0.9,0.95)、eps=1e-8、weight_decay=0.1、主干clip1.0均固定。两组随机初始化、全参数训练，不复用旧模型、不做LoRA。

每组恰1个epoch、1413更新（前1412更新各16窗，末次6窗），22,598训练窗、10,001,709词、16,325,414输入token、16,302,816监督token。固定0/250/500/750/1000/1250/1413各48窗过程面板，共336前向；仅做诊断，不挑checkpoint。

主结果只用唯一final1413的完整开发集：18,792窗、17,418,742监督token，token加权NLL与exp(NLL)。定义Δ₂=NLL_W₂−NLL_D₂；Δ₂<0称“第二初始化重现方向”，否则称未重现，不自动追加种子追逐显著结果。连续差值和PPL比均报告。训练末100固定1314–1413，分别报告token加权纯LM NLL/PPL与step PPL均值；末100不是独立开发集、100个种子或显著性样本。

顺序D训练→W训练→D完整dev→W完整dev。每训练stage软限10,700秒、硬限10,800秒；每dev软限7,100秒、硬限7,200秒；整队列硬限36,600秒（10小时10分，含收尾）。实际训练吞吐在新机器首批科学更新观测后报告，不能借用旧MIG测速。GB300原W完整评分两段已消耗4349.595秒，故新评测上限预先增至两小时。新硬限属于新批，不延长任何旧作业；任一stage不完整/失败即停止后续，保存已有证据，无自动resume/retry。

两组全部完成的预定科学账目：训练45,196F/45,196B/2,826updates；过程面板672F；完整dev37,584F；合计83,452F、45,196B。初始化构造、已有迁移tiny、重放均独立记账，不混入科学训练。

机器为已有共享GB300。使用既有项目GPU锁，保持其他任务原样。新卡费率未知，无法核实美元上限；不把未知写成0或套旧租价，保存起止UTC、每stage秒数与最大36,600秒执行授权。原20美元周期及原失败成本保留，本批单列；用户此前已明确不以预算余额核对阻塞。新训练引擎仅复制原引擎并添加“未知费率、硬时间限”行政模式，科学数学/数据/优化路径不变且逐差异审查。所有原冻结源码保持原字节。

关本机条件：远端detached父子进程身份、同组超时、项目锁、初始参数和实际科学更新均已读回，所有代码/数据/协议在远端。训练每250更新保存完整checkpoint，词数节点与final保存model快照，原始日志持续写远端；本机关闭期间不能声称已完成本机备份或会主动通知。开机后先采集、核验、再判断，不自动停共享主机。
