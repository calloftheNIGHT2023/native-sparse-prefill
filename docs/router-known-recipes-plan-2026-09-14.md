# 已有强配方的两组40轮对照

在新训练前固定。目的：确认当前完整教师KL的46轮达标延迟是否被既有方法消除，避免将已知训练配方包装为新贡献。不是论文新方法试验，不扩模型、不租新卡。

前作依据：MiniMax Sparse Attention §3.2/B.4，短期full attention + full KL，随后仅selected support KL；K-Sparse Attention作者README算法，选中索引器分数加到主注意力logits，使任务损失直接回传。这里是同一MQAR骨干上的token-level、r16、top8含自身/前一位置的适配，不是复现MSA的block/GQA大模型，也不是原KSA同维额外head/MLX内核。KSA式分支允许输入梯度，系数固定1，不加KL；warmup分支KL输入与教师detach。

两组：warm4_selectedkl_r16（前4轮完整注意力、之后36轮稀疏并在选中8位置计算KL）；ksa_additive_r16（从第一步稀疏、0教师）。均主干437760参数、索引器16维、训练seed123/索引器seed2026091470，与原full-KL r16用完全相同初始主干及索引器、10000训练/1000开发、batch32、40轮、AdamW/余弦Tmax100及lr/weightdecay，不调参不早停。策略导致dropout张量形状不同，不声称逐步随机样本相同。

原full-KL第40轮已完整归档，作为同初始化、同数据、同更新数的参考，省去重复训练；它来自上一张A40，不能用旧墙钟与新机直接声称加速。原80轮结果只作为额外训练的背景，不能混入40轮等预算胜出比较。

新增共25040主干优化步+25040索引器优化步、51200000输入tokens、3200000监督答案，两个新fit各12520对更新。科学训练已知总量；每组12分钟、整批30分钟硬上限。运行在现有A40，原总500美元、首阶段30美元限制不改变，实际费率与账单未知。

新测试seed2026091492，1024序列/4096答案、1024源值交换，运行前生成、标签及与旧训练/开发/各测试整行去重审计。三份40轮权重在两组拟合全部结束后统一评测，不用旧新题调停止点。主要门槛：新题和交换各>=99%；记录首次开发>=99轮、全部曲线、完整逐步日志、梯度/分数规模、优化器/检查点。完整QK教师去除只是计算图性质，尚不等于端到端实测加速，低维索引器本身仍为二次打分。

进入付费小批前：4项CPU测试和云端相同测试通过；CUDA前向/梯度隔离/有限梯度检查0更新。结束回传本机SHA与预测核验。如果既有配方解决延迟，则关闭将常规冷启动作为原创贡献的方向。若未解决，也不等于方法普遍无效；下一步需要独立训练种子及合理配置验证。
